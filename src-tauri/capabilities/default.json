{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main cmux Windows desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
